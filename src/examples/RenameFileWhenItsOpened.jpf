# Name of the main class to run in JPF
target = RenameFileWhenItsOpened

# Different file systems has different implementation of renaming (moving) an opened
# file. With a following config key BFS behavior in case of renaming opened file can be set.
# Possible values are error, warning and nothing.
jpf-bfs.opened_rename = error